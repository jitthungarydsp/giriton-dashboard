name: Half Hourly Courier Hub Sync

on:
  workflow_dispatch:

jobs:
  moved:
    name: Moved to HUB_JOB Courier Hub Sync
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow has been moved into HUB_JOB Courier Hub Sync."
          echo "Use .github/workflows/hub-job-courier-hub-shift-sync.yml instead."
